<?xml version="1.0"?>
<Plugin name="Restrict_Root_Remote_Login">
    <PluginVersion>1</PluginVersion>
    <PluginName>Restrict_Root_Remote_Login.xml</PluginName>
    <TargetOS>Linux</TargetOS>
    <Result_Type>action</Result_Type>
    <Info>Root 계정의 원격 접속 제한 설정</Info>
    <Description>Root 계정의 원격 접속을 제한하기 위해 SSH 설정 파일에 PermitRootLogin No를 추가</Description>
    <Commands>
        <Command>
            <CommandName>Set_Restrict_Root_Remote_Login</CommandName>
            <CommandType>Bash</CommandType>
            <CommandString>sudo sh -c 'echo "PermitRootLogin No" >> /etc/ssh/sshd_config'</CommandString>
        </Command>
    </Commands>
</Plugin>
